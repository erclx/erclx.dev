---
description: Require explicit confirmation before a multi-path deletion
---

# Destructive operations standards

## Confirmation

- Before a multi-path `rm` or `rm -rf`, list every target path in chat and wait for explicit confirmation
- Do not read "clean up X" as authorization for a blanket nuke. It authorizes only X
